@page "/"

<PageTitle>Inicio - SistemaFIME</PageTitle>

<div class="max-w-6xl mx-auto px-4 py-8">
    <!-- Hero Section -->
    <div class="text-center p-12 bg-gradient-to-br from-blue-500 to-blue-700 rounded-xl text-white mb-12 shadow-xl">
        <h1 class="text-4xl md:text-5xl font-bold mb-4">¡Bienvenido a SistemaFIME!</h1>
        <p class="text-xl opacity-90 mb-8 max-w-2xl mx-auto leading-relaxed">
            Sistema de gestión moderno desarrollado con Blazor Server y PostgreSQL
        </p>
        <div class="flex gap-4 justify-center flex-wrap">
            <a href="/dashboard" class="bg-white text-blue-600 px-6 py-3 rounded-lg font-semibold hover:bg-gray-100 transition-colors duration-200 no-underline shadow-lg">
                📊 Ver Dashboard
            </a>
            <a href="/fetchdata" class="bg-blue-800 text-white px-6 py-3 rounded-lg font-semibold hover:bg-blue-900 transition-colors duration-200 no-underline shadow-lg">
                🗄️ Probar Base de Datos
            </a>
        </div>
    </div>
    
    <!-- Features Section -->
    <div class="mb-12">
        <h2 class="text-center text-3xl font-bold text-gray-800 mb-8">Características Principales</h2>
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mb-8">
            @foreach (var feature in features)
            {
                <div class="bg-white p-8 rounded-xl shadow-lg text-center hover:transform hover:scale-105 transition-all duration-300 hover:shadow-xl">
                    <div class="text-5xl mb-4">@feature.Icon</div>
                    <h3 class="text-xl font-semibold text-gray-800 mb-2">@feature.Title</h3>
                    <p class="text-gray-600 leading-relaxed">@feature.Description</p>
                </div>
            }
        </div>
    </div>
    
    <!-- Info Section -->
    <div class="mb-12">
        <div class="bg-blue-50 border-l-4 border-blue-400 p-6 rounded-lg shadow-md">
            <div class="flex items-start">
                <div class="text-2xl mr-3">💡</div>
                <div>
                    <h3 class="text-lg font-semibold text-blue-800 mb-2">¿Cómo funciona?</h3>
                    <p class="text-blue-700 leading-relaxed">
                        Esta aplicación utiliza <span class="font-semibold">Blazor Server</span> para ejecutar código C# en el servidor
                        y actualizar la interfaz de usuario a través de una conexión SignalR en tiempo real.
                        Los datos se almacenan en <span class="font-semibold">PostgreSQL</span> y todo se ejecuta en contenedores Docker.
                    </p>
                </div>
            </div>
        </div>
    </div>
    
    <!-- Stats Section -->
    <div class="bg-white rounded-xl p-8 shadow-lg">
        <div class="grid grid-cols-1 sm:grid-cols-3 gap-8 text-center">
            @foreach (var stat in stats)
            {
                <div class="group">
                    <div class="text-3xl font-bold text-blue-600 mb-2 group-hover:text-blue-700 transition-colors duration-200">@stat.Value</div>
                    <div class="text-gray-500 text-sm font-medium uppercase tracking-wide">@stat.Label</div>
                </div>
            }
        </div>
    </div>
</div>

@code {
    private DateTime connectionTime = DateTime.Now;

    private List<FeatureItem> features = new()
    {
        new FeatureItem { Icon = "⚡", Title = "Blazor Server", Description = "Interfaz de usuario interactiva con C# en tiempo real" },
        new FeatureItem { Icon = "🗄️", Title = "PostgreSQL", Description = "Base de datos robusta y escalable para tus datos" },
        new FeatureItem { Icon = "🎨", Title = "CSS Vanilla", Description = "Diseño limpio sin dependencias externas" },
        new FeatureItem { Icon = "🐳", Title = "Docker", Description = "Despliegue fácil y consistente en cualquier entorno" },
        new FeatureItem { Icon = "📱", Title = "Responsive", Description = "Funciona perfectamente en dispositivos móviles" },
        new FeatureItem { Icon = "🔒", Title = "Seguro", Description = "Implementa las mejores prácticas de seguridad" }
    };

    private List<StatItem> stats = new();

    protected override void OnInitialized()
    {
        connectionTime = DateTime.Now;
        stats = new List<StatItem>
        {
            new StatItem { Value = DateTime.Now.Year.ToString(), Label = "Año actual" },
            new StatItem { Value = connectionTime.ToString("HH:mm"), Label = "Hora del servidor" },
            new StatItem { Value = "v1.0", Label = "Versión" }
        };
    }

    public class FeatureItem
    {
        public string Icon { get; set; } = "";
        public string Title { get; set; } = "";
        public string Description { get; set; } = "";
    }

    public class StatItem
    {
        public string Value { get; set; } = "";
        public string Label { get; set; } = "";
    }
}